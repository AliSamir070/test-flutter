import 'package:flutter/material.dart';

class ShowScreen extends StatelessWidget {
  static String routename = "ShowScreen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
